name: Push

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  test:
    uses: ./.github/workflows/_test.yml

  build:
    uses: ./.github/workflows/_build.yml
    secrets: inherit

